on:
  # This allows manual activation of this action for testing.
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  INTEGRATION_TEST_INSTANCE: ${{secrets.INTEGRATION_TEST_INSTANCE}}

jobs:
  build:
    name: Integration Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
    - name: checkout
      uses: actions/checkout@v4

    - name: set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'

    - name: build
      run: go build -v ./...
    
    - id: 'auth'
      if: ${{ secrets.SPANNER_CASSANDRA_ADAPTER_CICD_SERVICE_ACCOUNT != '' }}
      uses: 'google-github-actions/auth@v2'
      with:
          credentials_json: '${{ secrets.SPANNER_CASSANDRA_ADAPTER_CICD_SERVICE_ACCOUNT }}'

    - name: skip integration tests when credentials are unavailable
      if: ${{ secrets.SPANNER_CASSANDRA_ADAPTER_CICD_SERVICE_ACCOUNT == '' }}
      run: echo "Skipping integration tests because SPANNER_CASSANDRA_ADAPTER_CICD_SERVICE_ACCOUNT secret is not available."

    - name: it tests against Spanner 
      if: ${{ secrets.SPANNER_CASSANDRA_ADAPTER_CICD_SERVICE_ACCOUNT != '' }}
      run: |
          go mod tidy
          go test -v  ./... -tags=integration

    - name: it tests against Cassandra 
      if: ${{ secrets.SPANNER_CASSANDRA_ADAPTER_CICD_SERVICE_ACCOUNT != '' }}
      run: |
          go mod tidy
          go test -v  ./... -tags=integration -target=cassandra
